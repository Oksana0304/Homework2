package com.company.machines;

import com.company.Transport;

public class Vehicle extends Transport {

    public Vehicle(int fuelVolume, int maxSpeed, int amorlevel) {
        super(fuelVolume, maxSpeed, amorlevel);
    }
}
